{
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "npm install",
  "outputDirectory": ".next",
  "functions": {
    "app/**/*.{ts,tsx,js,jsx}": { "regions": ["hnd1"] },
    "middleware": { "regions": ["hnd1"] }
  }
}
